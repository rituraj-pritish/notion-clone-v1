import Button from 'atoms/Button';
import { NoAuthNavbar } from 'components';
import type { NextPage } from 'next';

const Home: NextPage = () => {
	return (
		<NoAuthNavbar/>
	);
};

export default Home;
